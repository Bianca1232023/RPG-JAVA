public interface Interface_inventario{
    Boolean removeritem(String item);
    void additem(String item);
    //ela define quais métodos uma classe deve ter, mas não implementa esses métodos(como se fosse um contrato)
}
